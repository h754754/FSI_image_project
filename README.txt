To test the code, one must use pipeline.m, in which there are instructions about where to insert the text image.

In case you want to create again the alphabet, you can use buildTemplates.m, indicating as first parameter the filename of the alphabet (alphabet_bold.bmp) and as second parameter 'A':'Z', which generates a list with all the characters from A to Z.